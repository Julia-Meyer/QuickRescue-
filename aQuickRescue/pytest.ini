[pytest]
# Pytest Configuration - Speckit Compliance
# QuickRescue Testing Standards

# Test discovery
testpaths = packages/backend/tests backend/tests
python_files = test_*.py *_test.py
python_classes = Test*
python_functions = test_*

# Output options
addopts =
    -v
    --strict-markers
    --tb=short
    --cov=packages/backend/app
    --cov=backend/app
    --cov-report=html:htmlcov
    --cov-report=term-missing:skip-covered
    --cov-report=xml
    --cov-fail-under=80
    -ra

# Coverage options
[coverage:run]
source = packages/backend/app,backend/app
branch = True
omit =
    */tests/*
    */migrations/*
    */__pycache__/*
    */venv/*

[coverage:report]
exclude_lines =
    pragma: no cover
    def __repr__
    raise AssertionError
    raise NotImplementedError
    if __name__ == .__main__.:
    if TYPE_CHECKING:
    @abstractmethod
    @abc.abstractmethod

# Markers
markers =
    unit: Unit tests
    integration: Integration tests
    e2e: End-to-end tests
    slow: Slow tests
    security: Security tests
    asyncio: Async tests

# Async support
asyncio_mode = auto

